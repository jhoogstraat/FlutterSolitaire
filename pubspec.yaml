name: solitaire_flutter
description: A solitaire clone made in Flutter.

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  assorted_layout_widgets: ^5.0.2
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 1.0.4

flutter:
  uses-material-design: true

  assets:
    - assets/backgrounds/

    - assets/cards/01/
    - assets/cards/private/